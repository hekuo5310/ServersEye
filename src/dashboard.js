const escapeHtml = (value) => String(value ?? "").replace(/[&<>'"]/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", "\"": "&quot;" })[character]);

export function dashboardHtml({ admin = false } = {}) {
  const title = admin ? "ServersEye 管理后台" : "ServersEye 服务器状态";
  return `<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#ffffff"><title>${title}</title>
<style>
:root{--paper:#fff;--ink:#111214;--graphite:#555a60;--mist:#f2f3f4;--rule:#d4d7da;--offline:#9ca1a7;--radius:12px}*{box-sizing:border-box}html{background:var(--paper)}body{margin:0;background:var(--paper);color:var(--ink);font:15px/1.5 ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif}button,input{font:inherit}button:focus-visible,input:focus-visible,a:focus-visible{outline:3px solid var(--ink);outline-offset:3px}main{max-width:1180px;margin:0 auto;padding:30px 22px 72px}.masthead{display:grid;grid-template-columns:1fr auto;gap:24px;align-items:end;border-bottom:2px solid var(--ink);padding-bottom:19px}.eyebrow,.mono,.metric-label,th{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;letter-spacing:.06em;text-transform:uppercase}.eyebrow{font-size:11px;color:var(--graphite);margin-bottom:9px}.brand{display:flex;align-items:baseline;gap:13px}.brand h1{margin:0;font-size:clamp(28px,5vw,48px);line-height:.95;letter-spacing:-.07em}.brand span{font:11px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.08em;color:var(--graphite)}.page-copy{margin:12px 0 0;color:var(--graphite);max-width:560px}.nav-link{border:1px solid var(--ink);border-radius:9px;padding:8px 11px;color:var(--ink);text-decoration:none;font-size:13px}.nav-link:hover{background:var(--ink);color:var(--paper)}.readout{display:flex;justify-content:space-between;align-items:center;padding:17px 0 15px;border-bottom:1px solid var(--rule);gap:14px}.readout strong{font-size:14px}.muted{color:var(--graphite);font-size:13px}.node-list{display:grid;gap:12px;margin-top:18px}.probe{display:grid;grid-template-columns:10px minmax(0,1fr);border:1px solid var(--ink);border-radius:var(--radius);overflow:hidden;background:var(--paper)}.signal{position:relative;background:var(--ink)}.signal:after{content:"";position:absolute;left:3px;right:3px;top:13px;height:13px;border-radius:50%;background:var(--paper)}.probe.offline{border-color:var(--rule)}.probe.offline .signal{background:var(--offline)}.probe.offline .signal:after{background:var(--paper)}.probe-body{padding:18px 18px 16px}.probe-head{display:flex;justify-content:space-between;gap:16px;align-items:start}.node-name{font-size:20px;line-height:1.15;font-weight:720;letter-spacing:-.035em;overflow-wrap:anywhere}.status{flex:none;border:1px solid var(--ink);border-radius:999px;padding:3px 8px;font:11px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.06em}.status.online{background:var(--ink);color:var(--paper)}.status.offline{border-color:var(--offline);color:var(--graphite)}.last-seen{margin:9px 0 0}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));margin-top:20px;border-top:1px solid var(--rule)}.metric{padding:10px 12px 0 0;min-width:0}.metric+.metric{border-left:1px solid var(--rule);padding-left:12px}.metric-label{display:block;color:var(--graphite);font-size:10px}.metric b{display:block;margin-top:3px;font:600 clamp(17px,2.5vw,23px)/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:-.08em;white-space:nowrap}.empty{margin-top:18px;border:1px dashed var(--offline);border-radius:var(--radius);padding:28px;color:var(--graphite)}.console{margin-top:22px;border:1px solid var(--ink);border-radius:var(--radius);overflow:hidden}.console-tools{display:flex;gap:9px;align-items:center;flex-wrap:wrap;padding:13px;border-bottom:1px solid var(--ink);background:var(--mist)}button,input{border:1px solid var(--ink);border-radius:9px;background:var(--paper);color:var(--ink);padding:9px 11px}button{background:var(--ink);color:var(--paper);cursor:pointer}button:hover{opacity:.78}button.danger{background:var(--paper);color:var(--ink);border-color:var(--ink)}.count{margin-left:auto}.login{max-width:420px;margin-top:26px;border:1px solid var(--ink);border-radius:var(--radius);padding:22px}.login h2{font-size:20px;letter-spacing:-.04em;margin:0 0 5px}.login input{width:100%;margin:16px 0 10px}.login button{width:100%}.table-wrap{overflow:auto}table{border-collapse:collapse;width:100%;min-width:760px}th,td{text-align:left;border-bottom:1px solid var(--rule);padding:12px 14px;vertical-align:middle}th{font-size:10px;color:var(--graphite);font-weight:600}tbody tr:last-child td{border-bottom:0}.state-dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:var(--ink);margin-right:7px}.state-dot.offline{background:var(--offline)}.admin-status{font:12px ui-monospace,SFMono-Regular,Menlo,monospace}.danger[data-id]{padding:6px 9px;font-size:12px}@media (prefers-reduced-motion:no-preference){.signal:after{animation:probe-pulse 2.4s ease-in-out infinite}@keyframes probe-pulse{50%{transform:scale(.55);opacity:.35}}}@media(max-width:680px){main{padding:22px 15px 50px}.masthead{grid-template-columns:1fr;gap:17px}.brand h1{font-size:34px}.nav-link{justify-self:start}.readout{align-items:start;flex-direction:column}.metrics{grid-template-columns:1fr 1fr;gap:0}.metric:nth-child(3){border-left:0;padding-left:0;margin-top:13px}.metric:nth-child(4){margin-top:13px}.probe-body{padding:17px 14px 15px}.count{margin-left:0}}
</style></head><body><main>
<header class="masthead"><div><div class="eyebrow">ServersEye / probe network</div><div class="brand"><h1>${admin ? "管理控制台" : "服务器状态"}</h1><span>${admin ? "ADMIN" : "PUBLIC"}</span></div><p class="page-copy">${admin ? "管理已注册的探针与服务器记录。" : "来自 ServersEye 探针的最新心跳读数。"}</p></div><a class="nav-link" href="${admin ? "/" : "/admin"}">${admin ? "查看公开状态" : "管理员入口"}</a></header>
<section id="app" aria-live="polite"></section></main>
<script>
const isAdmin=${admin};const app=document.querySelector('#app');
const escapeHtml=${escapeHtml.toString()};
const number=(value,suffix='')=>value===null||value===undefined?'—':escapeHtml(value)+suffix;
const date=value=>value?new Date(value).toLocaleString('zh-CN',{hour12:false}):'尚未上报';
const fresh=value=>value&&Date.now()-value<90000;
function percent(value){return value===null||value===undefined?'—':Math.round(Number(value))+'%'}
function publicCard(agent){const m=agent.metrics||{},disk=m.disk||{},diskPercent=disk.used&&disk.total?Math.round(disk.used/disk.total*100):null;return '<article class="probe '+(agent.online?'online':'offline')+'"><div class="signal" aria-hidden="true"></div><div class="probe-body"><div class="probe-head"><div><div class="node-name">'+escapeHtml(agent.name)+'</div><div class="muted last-seen">最后心跳：'+date(agent.last_seen_at)+'</div></div><span class="status '+(agent.online?'online':'offline')+'">'+(agent.online?'在线':'离线')+'</span></div><div class="metrics"><div class="metric"><span class="metric-label">CPU</span><b>'+percent(m.cpu_percent)+'</b></div><div class="metric"><span class="metric-label">内存</span><b>'+percent(m.memory_percent)+'</b></div><div class="metric"><span class="metric-label">磁盘</span><b>'+percent(diskPercent)+'</b></div><div class="metric"><span class="metric-label">运行时长</span><b>'+number(m.uptime_seconds?Math.floor(m.uptime_seconds/3600):null,' h')+'</b></div></div></div></article>'}
function publicShell(data){const agents=data.agents||[],online=agents.filter(agent=>agent.online).length;return '<div class="readout"><div><strong>探针总览</strong><span class="muted">　'+online+' / '+agents.length+' 台在线</span></div><div class="mono muted">刷新于 '+date(Date.now())+'</div></div>'+(agents.length?'<div class="node-list">'+agents.map(publicCard).join('')+'</div>':'<div class="empty">暂时没有可展示的服务器。</div>')}
async function renderPublic(){try{const response=await fetch('/api/status');if(!response.ok)throw Error();app.innerHTML=publicShell(await response.json())}catch{app.innerHTML='<div class="empty">无法取得服务器状态，请稍后刷新。</div>'}}
function adminLogin(message=''){app.innerHTML='<form class="login" id="login"><div class="eyebrow">Restricted access</div><h2>管理员验证</h2><div class="muted">令牌仅保存在当前浏览器标签页。</div><input type="password" id="token" autocomplete="current-password" placeholder="ADMIN_TOKEN" aria-label="管理员令牌"><button>进入后台</button><div class="muted" style="margin-top:12px">'+escapeHtml(message)+'</div></form>';document.querySelector('#login').onsubmit=event=>{event.preventDefault();sessionStorage.setItem('serverseye-admin-token',document.querySelector('#token').value);renderAdmin()}}
function adminTable(agents){return '<div class="console"><div class="table-wrap"><table><thead><tr><th>服务器</th><th>状态</th><th>CPU</th><th>内存</th><th>最后心跳</th><th>操作</th></tr></thead><tbody>'+agents.map(agent=>{const m=agent.metrics||{},online=fresh(agent.updated_at);return '<tr><td><strong>'+escapeHtml(agent.name)+'</strong></td><td><span class="admin-status"><i class="state-dot '+(online?'':'offline')+'"></i>'+(online?'在线':'离线')+'</span></td><td>'+percent(m.cpu_percent)+'</td><td>'+percent(m.memory_percent)+'</td><td>'+date(agent.updated_at)+'</td><td><button class="danger" data-id="'+escapeHtml(agent.id)+'">删除</button></td></tr>'}).join('')+'</tbody></table></div></div>'}
async function renderAdmin(){const token=sessionStorage.getItem('serverseye-admin-token');if(!token)return adminLogin();try{const response=await fetch('/api/agents',{headers:{Authorization:'Bearer '+token}});if(response.status===401){sessionStorage.removeItem('serverseye-admin-token');return adminLogin('令牌无效。')}if(!response.ok)throw Error();const data=await response.json();app.innerHTML='<div class="readout"><div><strong>已注册探针</strong><span class="muted">　当前记录</span></div><div class="console-tools"><button id="refresh">刷新数据</button><button id="logout" class="danger">退出</button><span class="muted count">共 '+data.agents.length+' 台服务器</span></div></div>'+(data.agents.length?adminTable(data.agents):'<div class="empty">还没有被控服务器。请先运行安装脚本注册探针。</div>');document.querySelector('#refresh').onclick=renderAdmin;document.querySelector('#logout').onclick=()=>{sessionStorage.removeItem('serverseye-admin-token');adminLogin()};document.querySelectorAll('[data-id]').forEach(button=>button.onclick=async()=>{if(!confirm('确定删除这台服务器吗？被控端之后无法继续上报。'))return;const result=await fetch('/api/agents/'+button.dataset.id,{method:'DELETE',headers:{Authorization:'Bearer '+token}});if(!result.ok)return alert('删除失败');renderAdmin()})}catch{app.innerHTML='<div class="empty">无法加载管理数据。请确认 ADMIN_TOKEN 与 Worker 配置一致。</div>'}}
isAdmin?renderAdmin():renderPublic();if(!isAdmin)setInterval(renderPublic,20000);
</script></body></html>`;
}
